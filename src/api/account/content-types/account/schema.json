{
  "kind": "collectionType",
  "collectionName": "accounts",
  "info": {
    "singularName": "account",
    "pluralName": "accounts",
    "displayName": "Account"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "status": {
      "type": "enumeration",
      "enum": [
        "Enable",
        "Disable"
      ]
    },
    "register_date": {
      "type": "datetime"
    },
    "plan_type": {
      "type": "enumeration",
      "enum": [
        "guest",
        "daily",
        "month",
        "year",
        "free"
      ]
    },
    "payment": {
      "type": "decimal"
    },
    "payday": {
      "type": "integer"
    },
    "payment_date": {
      "type": "date"
    },
    "expiration_day": {
      "type": "datetime"
    }
  }
}
